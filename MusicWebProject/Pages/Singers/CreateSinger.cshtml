@page
@model CreateSinger


<div class="d-grid">
    <form method ="Post">
        <div class="mb-4">
            <label asp-for="Singer.Name" class="form-label">Name</label>
            <input asp-for="Singer.Name" type="text" class="form-control" placeholder="Add Singer: SZA" />
        </div>
        <input class="btn btn-outline-primary" type="submit" value="Add" />
    </form>
</div>
